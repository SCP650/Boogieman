using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestDataTracking : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        DataTracker.test_save();
    }
}
